name: exchange_repository
description: Dart package which manages the currency exchange rates.
version: 1.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.1.8
  http: ^0.13.4
